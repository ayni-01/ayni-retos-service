spring:
  application:
    name: ayni-retos-service
  datasource:
    url: jdbc:postgresql://localhost:5432/somosayni
    username: ${DB_USERNAME:somosayni}
    password: ${DB_PASSWORD:somosayni123}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

server:
  port: 8083

jwt:
  secret: ${JWT_SECRET:somosayni-jwt-secret-key-que-debe-ser-muy-larga-para-hs256-algoritmo-seguro}
  expiration-ms: ${JWT_EXPIRATION:86400000}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.somosayni.retos: DEBUG
