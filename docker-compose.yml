version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: ayni-retos-db
    environment:
      POSTGRES_DB: somosayni
      POSTGRES_USER: somosayni
      POSTGRES_PASSWORD: somosayni123
    ports:
      - "5434:5432"
    volumes:
      - retos_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U somosayni"]
      interval: 10s
      timeout: 5s
      retries: 5

  retos-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ayni-retos-service
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_USERNAME: somosayni
      DB_PASSWORD: somosayni123
      JWT_SECRET: ${JWT_SECRET}
    ports:
      - "8083:8083"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  retos_data:
